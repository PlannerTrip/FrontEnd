import React from "react";
import { View } from "react-native";
import { Plan } from "../../interface/planSelect";

const PlanCard = ({ plan }: { plan: Plan }) => {
  return <View className="w-[358px] p-[16px] bg-white mt-[10px]"></View>;
};

export default PlanCard;
